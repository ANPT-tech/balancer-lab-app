services:
  backend-1:
    build: .
    environment:
      INSTANCE_ID: backend-1
    ports:
      - "8001:8000"
    restart: unless-stopped

  backend-2:
    build: .
    environment:
      INSTANCE_ID: backend-2
    ports:
      - "8002:8000"
    restart: unless-stopped
